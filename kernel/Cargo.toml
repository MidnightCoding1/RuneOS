[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
limine = "0.6.3"

[dependencies.x86_64]
version = "0.15.4"
default-features = false
features = [
    "instructions",
    "abi_x86_interrupt",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
